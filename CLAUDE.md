# CLAUDE.md — lunarlog

@AGENTS.md

[`AGENTS.md`](AGENTS.md) is the single operating guide for this repository
(project context, Supabase backend, credential locations, build and
verification instructions). This file is a pointer only — keep it that way
and edit `AGENTS.md` instead, so the two can never drift (issue #99).
