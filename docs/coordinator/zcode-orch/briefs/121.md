# Brief — #121 simulator lifecycle hang (QUOTA-KILLED ~19:19Z after 20min; resume .worktrees/claude-orch/121-simulator-lifecycle — HEAD 0183d0d9 "fix(testing): real-simulator lifecycle tr..." — partial work IS committed, assess git log -p first)
Root cause (issue body): transitionTo → handleAppLifecycleStateChanged(paused/hidden) on IntegrationTestWidgetsFlutterBinding triggers REAL OS suspension; VM-service-driven test hangs forever. CI ios-simulator job (self-hosted Mac) sidesteps with --plain-name filters (4 safe tests).
Fix directions (issue's own): (1) preferred — research pinned Flutter 3.47.2 integration_test package (pub cache) for a supported lifecycle simulation; (2) else xcrun simctl-driven real backgrounding coordinated from the CI job script (test polls/reconnects). Host-mode behavior must stay byte-identical (test/ui/gate_test.dart green). Acceptance: the two paused/hidden tests run on the real simulator in CI (--plain-name scoping shrinks/disappears) OR documented evidence trail + keep scoping (no fake passes). ENSURE the ios-simulator job/step has a bounded timeout so a hang fails visibly instead of parking the Mac runner. Verification loop = CI on the PR (no local iOS sim on Windows). Gates: analyze, full flutter test, host-mode integration cases. PR: owner:zcode-orch label, head claude-orch/121-simulator-lifecycle (grandfathered), Closes #121 (or Addresses if documented-fallback).
