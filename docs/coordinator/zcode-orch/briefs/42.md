# Brief — #42 sync batching (QUOTA-KILLED 2nd time ~19:19Z after 101min — worktree still at BASE commit b6991074 with zero dirty files after BOTH sessions; investigate git stash list and reflog before assuming nothing exists, then continue or restart)
Same brief as docs/coordinator/briefs/42.md (still valid): batched markPushed preserving local_rev; per-page IN(...) lookups in remote apply (SQLite var cap); max(server_version) probe before daily reconcile (error → full re-pull). Tests for all three + local_rev guard. Gates: analyze, full flutter test, quality_gate (lib/ floor+CRAP). PR: owner:zcode-orch label, head claude-orch/42-sync-batching (grandfathered), Closes #42.
