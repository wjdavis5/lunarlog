Worktree .worktrees/claude-orch/183-bc-reminders, branch claude-orch/183-bc-reminders, from current main. Issue #183 (read fully, `gh issue view 183`) is the spec: birth-control adherence reminders with per-method cadence. Deps: #260 (birth-control tracking) JUST CLOSED — its canonical vocabulary (lib/domain/birth_control.dart, six methods, birthControlMethodInEffectOn resolver) and the #136 per-type reminder architecture are your foundations; #188's profile_modes.birth_control_method holds the current method. Build on both; never fork. The disabled birth-control placeholder row from #136's settings layout is yours to enable. ARB copy, generic notification text, per-method cadence config per the issue.
Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`.
PR: `gh pr create --title "feat(notifications): birth-control adherence reminders with per-method cadence (#183)"` — body: Closes #183 / What changed / How it satisfies the acceptance criteria / Tests / Not done.
