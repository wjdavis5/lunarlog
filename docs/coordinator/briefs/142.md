# Task brief — Issue #142: amend privacy commitment — remove the no-fertility-tracking exclusion

Worktree: `C:/git/repos/lunarlog-wt/142-privacy-fertility-amendment`
Branch: `feat/142-privacy-fertility-amendment` (created, based on origin/main)
Issue: https://github.com/wjdavis5/lunarlog/issues/142

## Context you must respect

The product owner has DECIDED (issue #142/#123): the no-fertility-tracking commitment is deprecated; the target is full parity with a full cycle-tracking product, including ovulation/fertile-window estimation, on all profiles including `isMinor` ones. Your job is ONLY the policy/copy amendment — the estimation features themselves are separate open issues (#143, #144) and are NOT being built here.

**Truthfulness constraint (coordinator decision):** the fertility features do not exist in the shipped code yet. The rewritten copy must not claim capabilities the app does not have today. State accurately: the app tracks cycles/flow and estimates the next period today; fertility-related estimation is an intended part of the product's scope (remove the guarantee that it will never exist). Do not write "the app estimates fertile windows" as a present-tense feature claim anywhere user-facing.

## Acceptance criteria (from the issue)

1. No occurrence of the no-fertility-tracking commitment remains as a guarantee in `PRIVACY.md`, `README.md`, `AGENTS.md`, in-app copy, or the named code comments. Locations (verify each, grep for `fertility`/`Fertility` case-insensitively across the repo):
   - `PRIVACY.md:17` ("No Fertility Tracking or Algorithms" Core Principle)
   - `README.md:15` and `README.md:313` ("No fertility features, by design")
   - `lib/domain/tags.dart:4` header comment — see note below
   - `lib/domain/prediction/prediction.dart:9` ("no fertility-phase wording anywhere (R13)")
   - `lib/ui/settings/settings_screen.dart` — the in-app trust-signal copy asserting no fertility algorithms
   - `docs/plans/2026-09-04-0738-feat-target-state-roadmap-plan.md` — the "Outside this product's identity" section and the PRIVACY.md authority note
   - `AGENTS.md` — grep for any remaining fertility-exclusion statement
2. `PRIVACY.md` gets an updated `Last Updated`/`Effective Date` and a change-history line stating plainly that the no-fertility promise was narrowed/removed and why (owner decision, full target-state parity).
3. Guardian-visibility and ownership-transfer behavior for (future) fertility data is explicitly stated in `PRIVACY.md`: fertility-related data/estimates, when they exist, follow the existing guardian/profile permission model (a guardian who can see a profile's cycle data sees its estimates; data transfers with the profile in ownership transfers). No special-case handling.
4. Add a comment on issue #21 noting the App Store privacy details / Play Data safety answers must be updated for reproductive/fertility data (use `gh issue comment 21 --body "..."`).
5. The roadmap plan doc no longer lists the no-fertility scope as a fixed boundary.

**Note on `lib/domain/tags.dart`:** the issue says remove the "No fertility-related vocabulary exists" comment "once the taxonomy actually gains fertility-related tags (coordinate with the fertility-signal-logging feature issue)". That feature (#144) has NOT landed. Coordinator decision: reword the comment to state the CURRENT truth without re-committing to the old guarantee — e.g. note that the taxonomy is currently cycle/flow-only and fertility-signal tags arrive with #144. Do NOT add fertility tags in this PR.

Similarly `lib/domain/prediction/prediction.dart`: update the wording so it no longer promises "no fertility wording ever", but do not add fertility estimation logic.

## Commands (Windows Git Bash)

```bash
export PATH="/c/src/flutter/bin:$PATH"
flutter pub get && flutter analyze && flutter test   # must pass (comment/copy edits must not break anything)
```

## PR

`gh pr create --title "docs(privacy): remove the no-fertility-tracking commitment (#142)"` — body template:

```
Closes #142

## What changed
## How it satisfies the acceptance criteria
- [ ] AC1 — <how>
## Tests
## Not done
```
