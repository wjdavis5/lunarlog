# Task brief — Issue #160: localization scaffolding

Worktree: `C:/git/repos/lunarlog-wt/160-l10n-scaffold` — Branch: `feat/160-l10n-scaffold` (created from current origin/main)
Issue: https://github.com/wjdavis5/lunarlog/issues/160 — read it fully (`gh issue view 160`); it cites exact current-code locations.

## Scope (per the issue; the four-screen extraction is the deliverable, not all 313 sites)

1. `flutter_localizations` (SDK) + `intl`; `l10n.yaml`; `lib/l10n/app_en.arb`; gen via `flutter gen-l10n` (commit generated files per repo convention for generated code).
2. Register delegates + `supportedLocales: [Locale('en')]` on BOTH `MaterialApp`s (`lib/app.dart`, `lib/ui/gate/lock_screen.dart`).
3. Extract strings from the four highest-traffic screens (calendar, day sheet, overview, settings) to the ARB. Remaining sites are declared follow-on work — list the count you measured at PR time.
4. **Shared date formatter** (coordination point for #198, dispatching in parallel): add a small helper (e.g. `lib/ui/l10n/dates.dart` or similar) over `intl` `DateFormat` exposing human-readable forms ("Today · Tue 8 Sep", "Yesterday", "Tue 8 Sep 2026") usable with only `en` for now. Keep it importable without the rest of the l10n machinery.
5. Locale-derived month/weekday names + first-day-of-week seam (replace `kMonthNames`/`kWeekdayLabels` hand-rolled constants in `month_calendar.dart` and their re-import in `overview_panel.dart`; make Sunday-start an explicit, overridable constant rather than `weekday % 7` arithmetic).
6. Do NOT attempt RTL flips or the `Directionality` root change in this issue (tracked separately in the issue text) — but do not add new obstacles to it.

## Constraints

- `lib/domain` stays pure Dart — no Flutter/intl imports there; the formatter lives UI-side.
- Widget tests that assert on extracted English strings must keep passing (ARB values identical to current copy).
- No behavior changes beyond locale plumbing.

## Gates

```bash
export PATH="/c/src/flutter/bin:$PATH"
flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart
```

Plan doc under `docs/plans/` per house convention.

## PR

`gh pr create --title "feat(l10n): localization scaffolding — delegates, ARB, locale-derived calendar names, shared date helper (#160)"`, body: `Closes #160 / ## What changed / ## How it satisfies the acceptance criteria / ## Tests / ## Not done`.
