Worktree C:/git/repos/lunarlog-wt/211-android-hardening, branch feat/211-android-hardening, from current main. Issue #211 (read fully, `gh issue view 211`) is the spec: enable release minification/obfuscation and fix Sentry mapping upload. No schema/server changes. Verify the release build still passes (CI's android job builds debug; a local `flutter build apk --release` IS possible on this machine — run it to prove minification doesn't break the build), and keep the AGENTS.md Android-build note (which documents the sentry_flutter Kotlin-plugin warning and the deliberate no-minify posture) updated to the new reality.
Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && flutter build apk --release && dart run tool/quality_gate.dart`.
PR: `gh pr create --title "feat(android): enable release minification/obfuscation and fix Sentry mapping upload (#211)"` — Closes #211 / What changed / ACs / Tests / Not done.
