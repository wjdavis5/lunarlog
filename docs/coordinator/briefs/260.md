Worktree C:/git/repos/lunarlog-wt/260-birth-control, branch feat/260-birth-control, from current main. Issue #260 (read fully, `gh issue view 260`) is the spec: birth-control tracking — six methods and per-day intake. Deps #240 ✓ and #188 ✓ are landed (profile_modes.birth_control_method exists — coordinate, don't duplicate). Per-day intake likely means an observations category or a dedicated intake record — follow the issue's direction; schema bump => FULL procedure (build_runner + drift_schema_v<N+1>.json + ci.yml dump-filename bump — use a timestamp sorting after EVERYTHING on main, check the tip first; three same-timestamp collisions happened this week). Sync containment guards, pgTAP round-trip + intake semantics, ARB copy, reminder hookup is #183's (leave seams). Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`; pgTAP via Docker or CI-noted. Plan doc per house convention. PR: `gh pr create --title "feat(logging): birth-control tracking — six methods and per-day intake (#260)"` — Closes #260 / What changed / ACs / Tests / Not done.
