# Task brief — Issue #133: twelve-month forecast calendar with symptom layers

Worktree: `C:/git/repos/lunarlog-wt/133-forecast-calendar` — Branch: `feat/133-forecast-calendar` (created from origin/main)
Issue: https://github.com/wjdavis5/lunarlog/issues/133 — the issue body is the spec (R1/R2/R3, U3). Read it fully first (`gh issue view 133`).

## Coordinator notes (binding)

1. **Its "No fertility or ovulation overlays, per PRIVACY.md" constraint is STALE policy** — #142 removed that guarantee and #143 (open) owns fertility display. For THIS issue the practical scope stands unchanged (bleed-estimate bands + symptom layers only); do not reintroduce no-fertility copy anywhere, and do not build fertility overlays here either.
2. The sequencing dependency ("sequence after the cycle-history/confidence work") is satisfied: #132 just landed — `ActivePrediction`/confidence (`high|learning|irregular`) is available via `lib/domain/prediction/prediction.dart` and the `CycleHistoryService` from #132. Bands must render with confidence-appropriate visual weight.
3. **Semantics, not just visuals**: predicted vs logged days must be distinguishable by screen readers (Semantics labels) — the a11y issue (#138) will build on this; make the distinction semantic now. Both themes.
4. Future cells stay non-loggable (read-only explainer only). No new persisted state — derive per-cell flags from the existing streams. Keep the symptom-query shape reusable.

## Starting points

`lib/ui/logging/month_calendar.dart` (forward-navigation cap at ~L287 `isFuture`; entry stream), `lib/domain/prediction/prediction.dart` (estimate + `kSkipAdvanceCycles`), `lib/domain/prediction/cycle_history.dart` (#132, confidence + history), `lib/domain/models/day_entry.dart`/`lib/domain/tags.dart` (symptom layers default to most-used tags), `lib/ui/overview/overview_panel.dart` (visual language). Widget tests mirror in `test/ui/logging_test.dart` style; domain derivation (band computation, badge offsets) belongs pure-Dart in `lib/domain/` with its own tests.

## Gates (must pass before the PR)

```bash
export PATH="/c/src/flutter/bin:$PATH"
flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart
```

No schema/migration changes expected (derive-only). Plan doc under `docs/plans/` per house convention.

## PR

`gh pr create --title "feat(calendar): twelve-month forward calendar with predicted bleed bands and symptom layers (#133)"`, body template: `Closes #133 / ## What changed / ## How it satisfies the acceptance criteria (checklist) / ## Tests / ## Not done`.
