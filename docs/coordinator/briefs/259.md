Worktree C:/git/repos/lunarlog-wt/259-tracking-prefs, branch feat/259-tracking-prefs, from current main. Issue #259 (read fully, `gh issue view 259`) is the spec: per-profile tracking preferences (enabled-category set + sort order), SYNCED (guardians share a profile), data-model and read-path half only (#234 owns picker UI). The taxonomy is NOW expanded (#371 landed: 45 codes/15 categories incl. zero-code unverified categories — read the landed shape first). Storage choice is the implementer's per the issue (profiles.tracking_preferences jsonb or dedicated table — document it); default-enabled for new categories EXCEPT partying/sex_life hidden on isMinor profiles per the issue. Day sheet reads the preference for render set/order; disabled never deletes logged data. Schema bump => FULL procedure incl. ci.yml dump-filename bump. pgTAP for the sync path + minor-visibility default. Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`. Plan doc per house convention. PR: `gh pr create --title "feat(logging): per-profile tracking preferences — enable and reorder categories (#259)"` — Closes #259 / What changed / ACs / Tests / Not done.
