Worktree C:/git/repos/lunarlog-wt/208-flutter-pin, branch feat/208-flutter-pin, from current main. Issue #208 (read fully, `gh issue view 208`) is the spec: pin the Flutter SDK version in CI and add a Gemfile for fastlane. CI-only + tooling changes: pin via flutter-action's flutter-version input (read the exact version from AGENTS.md's documented SDK — 3.47.2 stable / Dart 3.13.2), add the Gemfile + Gemfile.lock with the fastlane gems the Fastfile needs, keep every workflow's behavior otherwise identical. Verify actionlint passes (the release-guard job runs it) and YAML is valid.
Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test` (CI workflows themselves are validated by the actionlint job in CI).
PR: `gh pr create --title "ci(release): pin Flutter SDK version and add a Gemfile for fastlane (#208)"` — Closes #208 / What changed / ACs / Tests / Not done.
