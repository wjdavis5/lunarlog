# Brief — #174 FCM presentation (QUOTA-PAUSED 2026-09-15 11:39Z; resume .worktrees/claude-orch/174-fcm-presentation)
1. @pragma('vm:entry-point') top-level background handler registered before runApp.
2. onMessage → present via flutter_local_notifications using ONLY fixed generic copy kReminderTitle/kReminderBody (lib/data/notifications/scheduling.dart) — never payload fields.
3. iOS setForegroundNotificationPresentationOptions(alert/badge/sound).
4. Android manifest meta-data: default_notification_channel_id → existing Reminders channel; default_notification_icon → existing monochrome icon.
5. Back the remote-notification UIBackgroundModes entry (handler now exists).
Everything behind AppConfig.hasPush; CI has NO FCM defines — code + tests must pass without Firebase init. Data→presentation mapping as pure tested functions (discretion pinned by test: health-looking payload still yields fixed copy). Device banner/delivery = manual checklist items (add to docs/ops/supabase-go-live.md device section, additive). Gates: analyze, full test, quality_gate. PR: owner:claude-orch, Closes #174.
