# Brief — #693 prediction horizon (QUOTA-PAUSED 2026-09-15 11:40Z; resume .worktrees/claude-orch/693-prediction-horizon — died ~7min in, little work)
Engine forecast (lib/domain/prediction/prediction.dart) sized by HORIZON not fixed 12 cycles: predict until predictedStart passes today+12 months, bounded cycle count (~24) documented; keep sqrt(cycleIndex) degradation exactly; public surface stable (grep call sites incl. tool/seed_test_accounts + reminder_window_publisher if changed). Tests: 21d and 35d profiles span month 12; tail tier matches formula; degenerate inputs bounded; existing tests (update only a test pinning the 12-cycle cap, with #693 comment). Gates: analyze, full test, quality_gate. PR: owner:claude-orch, Closes #693.
