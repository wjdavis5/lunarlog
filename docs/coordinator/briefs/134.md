# Task brief — Issue #134: deeper, customizable tracking taxonomy with intensity

Worktree: `C:/git/repos/lunarlog-wt/134-taxonomy` — Branch: `feat/134-taxonomy` (created from origin/main)
Issue: https://github.com/wjdavis5/lunarlog/issues/134 — the issue body is the spec. Read it fully first (`gh issue view 134`).

## Coordinator notes (binding)

1. **Codes are permanent; unknown codes never drop** (issue constraint) — these two invariants get regression tests.
2. **Intensity storage design** (settled direction): keep `tags` a plain string-code array (the server's `is_valid_tags_array` and all sync paths stay untouched) and store intensity OUT-OF-LINE, device-interchangeably: extend the day-entry payload with an optional `intensities` map (`code -> 1|2|3`) that rides sync like any other day-entry column — server migration adds the column (jsonb, CHECK: object of known-code-free string keys to smallints 1-3, default '{}'), joins `sync_push`'s day-entry key allowlist/insert/update (with the `v_row ? 'intensities'` containment guard per the #108 precedent), and the client codec/storage round-trip it. Legacy entries with no map are unambiguously "no intensity recorded". If your analysis finds a materially better shape that still honors "tags stay a string array", document the choice in the PR — but do NOT overload code strings with suffixes.
3. **Severity proxy replacement**: once intensity exists, replace the `flow = 'heavy'` high-severity fallback in `enqueue_caregiver_alerts` (migration `20260906220000`, current definition as re-emitted by `20260908110000`) with a real severity signal (high-intensity pain entry OR heavy flow — keep heavy flow as a legacy fallback so old entries still classify), in a NEW migration `create or replace`-ing the trigger function, with pgTAP updates. Rebase on the CURRENT function definition — check every migration that last defined it.
4. **Per-profile custom-tag registry**: stored per profile, synced opaquely — pragmatic shape: a `custom_tags` jsonb column on `profiles` (array of {code, display}, capped e.g. 20, codes snake_case-validated client-side, server CHECK shape-only) riding the existing profile column path exactly like `mode` did in `20260908120000` (grant + allowlist + containment guard). Case-insensitive dedupe, per-profile cap.
5. **Curation** (category on/off + order) is device-local per profile (`app_settings`), like #132's omission list — accepted divergence, captioned.
6. Care-mode interplay (#131, just merged): the surfaced set can vary by mode — wire the seam, don't redesign modes.
7. New migration filenames must sort after the current tip (≥20260908120000). Run `dart run build_runner build --delete-conflicting-outputs` and commit generated code.

## Starting points

`lib/domain/tags.dart` (taxonomy + `validateTagCodes`), `lib/domain/limits.dart` (tag bounds — server CHECKs must stay satisfied; element length 64 applies to custom codes too), `lib/domain/models/day_entry.dart`, `lib/data/db/tables.dart`/`storage.dart`/`db.dart` (schema v-next), `lib/data/sync/row_codec.dart`, `lib/ui/logging/day_sheet.dart` (chip grid — note #237's unrecognized-tags handling on main: your custom codes must render sanely alongside it), migration precedents `20260906160000` (profile column + containment guard), `20260908120000` (mode), `20260908110000` (current enqueue_caregiver_alerts). pgTAP: `supabase/tests/sync_push_test.sql`, `notification_outbox_test.sql`, `alert_digest_test.sql`.

## Gates (must pass before the PR)

```bash
export PATH="/c/src/flutter/bin:$PATH"
flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart
# pgTAP needs Docker; if unavailable rely on CI db-tests and note it in the PR
```

Update AGENTS.md schema lines for each new migration. Plan doc under `docs/plans/`.

## PR

`gh pr create --title "feat(logging): customizable taxonomy with intensity, custom tags, and curation (#134)"`, body template: `Closes #134 / ## What changed / ## How it satisfies the acceptance criteria (checklist) / ## Tests / ## Not done`.
