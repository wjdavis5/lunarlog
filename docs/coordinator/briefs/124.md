# Task brief — Issue #124: shared activity feed (what changed, who, when)

Worktree: `C:/git/repos/lunarlog-wt/124-activity-feed` — Branch: `feat/124-activity-feed` (created from origin/main)
Issue: https://github.com/wjdavis5/lunarlog/issues/124 — the issue body is the spec. Read it fully first (`gh issue view 124`).

## Coordinator notes (binding)

1. **No schema/sync changes and no new attribution model** — the feed is built from existing `logged_by_user_id` / `last_modified_by_user_id` / `updated_at` (+ `profile_guardians` display names) exactly as the issue constrains. Be honest about latest-modifier-only semantics in the UI (no fake audit trail).
2. **AC4 (same-date merge outcomes)** — pragmatic direction: detect discard events at apply time in the existing local apply paths (`lib/data/db/storage.dart` `_resolveSameDateConflicts` tombstones a local loser whose payload differed; `applyResolved` overwrites a differing local copy) and record a device-local merge-event row (e.g. an `app_settings` key per profile holding a bounded JSON list, idempotent by (entry id, updated_at), capped ~200, oldest evicted). Device-local only, never synced, never logged. If you find a cleaner honest way to satisfy AC4 within the no-schema constraint, take it and document the choice.
3. "New since you last opened this profile" marker is device-local (per-profile last-seen timestamp in `app_settings`).
4. Content discretion per the issue: rows carry change-kind and names, not note contents or health detail beyond calendar-glance level. Nothing here reaches notifications/lock screens/crash reports.
5. Feed lives behind the same providers the rest of the UI uses; a `viewer` guardian can read it; nothing in it mutates.

## Starting points

`lib/ui/logging/widgets/caregiver_attribution_badge.dart` (name resolution: you → display name → role label → "a guardian", never a uuid), `lib/data/db/storage.dart` (apply paths above; `watchDayEntries`/`getDayEntries` with tombstones for the change list), `lib/data/repositories/profile_guardians_repository.dart` + `storage.getGuardiansForProfile`, `lib/ui/profiles/profile_detail_screen.dart` and `lib/ui/sharing/manage_guardians_screen.dart` (entry points), `lib/ui/logging/day_sheet.dart` (tap-through target). Unread state: `lib/data/repositories/drift_settings_store.dart`.

## Gates (must pass before the PR)

```bash
export PATH="/c/src/flutter/bin:$PATH"
flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart
```

Plan doc under `docs/plans/` per house convention.

## PR

`gh pr create --title "feat(sharing): per-profile activity feed showing which guardian changed what (#124)"`, body template: `Closes #124 / ## What changed / ## How it satisfies the acceptance criteria (checklist) / ## Tests / ## Not done`.
