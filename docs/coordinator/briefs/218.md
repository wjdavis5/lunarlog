# Task brief — Issue #218: collect cycle facts at onboarding to seed provisional predictions

Worktree: `C:/git/repos/lunarlog-wt/218-provisional-seeding` — Branch: `feat/218-provisional-seeding` (from current origin/main)
Issue: https://github.com/wjdavis5/lunarlog/issues/218 — read it fully (`gh issue view 218`); its dependency #213 (confidence tiers) and #143 are landed, #188's mode storage exists.

BOUNDARY (coordinator, binding): #216 (onboarding rework, dispatched in parallel) owns the first-run FORM UI. You own the DOMAIN/DATA side: the `provisional` confidence tier, the seeding computation (onboarding facts -> provisional ActivePrediction-equivalent with honest uncertainty), storage of the collected facts (follow the issue's storage direction; device-local app_settings is acceptable ONLY if the issue allows — if the facts must sync, use the profile-scoped columns/tables the issue names), the refinement handoff (real cycles progressively displace the provisional baseline — the issue specifies the mechanics), and overview rendering hooks that consume the tier (labels via AppLocalizations, no hardcoded literals). If you need a minimal form-side hook, expose a clean seam (e.g. a service method the form will call) and document it — do NOT build the form UI.

Notes: prediction.dart is the heart — keep the empty-omission/no-facts path bit-identical (the #132 lesson); `kMinCompletedValidCycles` behavior for logged-in-anger profiles must not change. Disclaimer on every surfaced number. Tests: tier mapping, seeding math, refinement displacement, no-regression on the existing prediction suite.

Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`.
PR: `gh pr create --title "feat(prediction): provisional tier seeded from onboarding cycle facts (#218)"` — body: `Closes #218 / What changed / ACs checklist / Tests / Not done`. Plan doc per house convention.
