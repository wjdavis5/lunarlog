Worktree .worktrees/claude-orch/234-category-picker, branch claude-orch/234-category-picker, from current main. Issue #234 (read fully, `gh issue view 234`) is the spec: CategoryPicker with search/recents and an IntensitySelector. ALL deps landed: #371/#381 taxonomy (67 codes/22 categories), #256 intensity on observations, #259 tracking_preferences (curation/order), #341 autosave day sheet. Build the picker UI per the issue: search across display+codes, recents, collapsible clusters, intensity selector on pain-grade observations (rides the autosave into the intensity the observations model already stores). Respect #259's curated order/hidden categories. ARB copy; widget + unit tests; CRAP gate mindful (split helpers early).
Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`. Plan doc per house convention.
PR: `gh pr create --title "feat(logging): CategoryPicker with search/recents and an IntensitySelector (#234)"` — Closes #234 / What changed / ACs / Tests / Not done. Label: owner:claude-orch.
