Worktree C:/git/repos/lunarlog-wt/254-store-compliance, branch feat/254-store-compliance, from current main. Issue #254 (read fully, `gh issue view 254`) is the spec: health-platform store compliance. Deps #244/#156/#166 all closed. The in-repo deliverables: (1) the written 5.1.3 rule in the health-sync adapter (never write derived/predicted values — only user-logged/imported data), (2) PrivacyInfo.xcprivacy / manifest declaration updates tracking the health types, (3) docs (PRIVACY.md store-answers section + the issue's checklist). Anything requiring App Store Connect / Play Console console actions goes under "Not done" flagged for the human operator — do NOT pretend those are done. No schema changes expected. Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`. PR: `gh pr create --title "feat(privacy): health-platform store compliance declarations (#254)"` — Closes #254 / What changed / ACs / Tests / Not done.
