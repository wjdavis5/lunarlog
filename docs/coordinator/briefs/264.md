Worktree C:/git/repos/lunarlog-wt/264-delete-profile-rpc, branch feat/264-delete-profile-rpc, from current main. Issue #264 (read fully, `gh issue view 264`) is the spec: delete_profile_data() RPC purging a single profile. Dep #159 landed (provenance columns — the p_source variant is available). Server-side only (migration + RPC + pgTAP): SECURITY DEFINER, authority ladder per the issue (owner-only presumably — follow it exactly), purge scope = the profile's day_entries/observations/care notes/visit prep + guardian memberships + mode row, mapped to the landed #188/#128 tables; keep the account-deletion RPC (#17-era) untouched. RLS never weakened; revoke from anon; containment/tolerated-not-read not applicable (no sync of deletions — hard purge is the point). pgTAP: authority matrix, purge completeness per table, co-guardian's own data on OTHER profiles untouched. Timestamp: sort after the current tip (check it — several same-day migrations landed). Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`; pgTAP via Docker or CI-noted. Plan doc per house convention. PR: `gh pr create --title "feat(privacy): delete_profile_data RPC to purge a single profile (#264)"` — Closes #264 / What changed / ACs / Tests / Not done.
