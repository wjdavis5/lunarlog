Worktree C:/git/repos/lunarlog-wt/193-ios-health-writes, branch feat/193-ios-health-writes, from current main. Issue #193 (read fully via `gh issue view 193`) is the spec: write period days + flow level to Apple Health, one-way, opt-in, forward-only from grant; the full flow-enum mapping table as a pure unit-tested function in lib/data/health/ (native call sites stay excluded per #173). Deps #153/#156/#173/#180/#247 all CLOSED and landed — build on the #173 adapter (extend health_platform.dart port + iOS channel; do not fork). Native-side #153 guard already in the adapter — keep it forward. iOS compile verification is CI-only (no macOS here); say so under Not done. HealthConnect (Android) is #202's job — do not touch the Android adapter beyond what the shared port refactor requires.
Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`. Plan doc per house convention.
PR: `gh pr create --title "feat(health): write menstrual flow to Apple Health (#193)"` — Closes #193 / What changed / ACs / Tests / Not done.
