Worktree .worktrees/claude-orch/266-auth-config, branch claude-orch/266-auth-config, from current main. Issue #266 (read fully, `gh issue view 266`) is the spec: bring [auth] config under version control and CI enforcement. Server-side: the auth-config-as-code approach the issue directs (Supabase CLI config or declarative SQL — follow the issue; check what main's supabase/ structure + AGENTS.md Migration Flow say about non-migration config), plus a CI step that detects drift between the committed config and the project (using the existing supabase CLI setup; the deploy token may be absent in CI — design the step to skip-with-warning like Sentry's, or use the management API as the issue directs). Gates: standard four; pgTAP via Docker or CI-noted. Plan doc per house convention.
PR: `gh pr create --title "chore(supabase): bring [auth] config under version control and CI enforcement (#266)"` — Closes #266 / What changed / ACs / Tests / Not done. Label: owner:claude-orch.
