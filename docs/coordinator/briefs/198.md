# Task brief — Issue #198: day sheet — keyboard occlusion, silent data loss on dismiss, raw ISO dates

Worktree: `C:/git/repos/lunarlog-wt/198-day-sheet-ergonomics` — Branch: `fix/198-day-sheet-ergonomics` (created from current origin/main)
Issue: https://github.com/wjdavis5/lunarlog/issues/198 — read it fully (`gh issue view 198`); it cites exact lines and the repo's own correct precedents.

## Scope (per the issue's three defects)

1. **Keyboard-inset padding** on `_sheetShell`, matching the already-correct `accept_invite_sheet.dart`/`claim_profile_sheet.dart` pattern. Widget test with non-zero `viewInsets` asserting the Save affordance stays on screen.
2. **Autosave (preferred) or explicit guard** — coordinator decision: implement **autosave on change with debounce + "Saved" micro-confirmation** (the issue's preferred option, matching the commit-immediately model). Requirements: debounced write through the existing repository; the sheet never closes on save (no pop) — closing happens by dismissal; a transient "Saved" indicator; failures surface the existing inline retry error and KEEP the pending state (never silently drop); the delete flow keeps its existing confirm. Pin the Save/Delete affordances (or their autosave-era equivalents: delete + status indicator) in a persistent bottom area inside the sheet, not scrollable away. Add a `PopScope` guard only for the failure-pending state (unsaved-because-failed), not for normal autosaved dismissal.
3. **Human-readable dates**: `day_sheet.dart` title, delete-confirmation body, read-only variant. Coordination: #160 (parallel dispatch) adds the shared `intl`-based helper — until it lands, add `intl` usage behind a tiny local helper (same API shape) and note in the PR that #160's helper absorbs it; if #160 has already landed on your branch's base, use it directly.

## Constraints

- Day-entry writes go through the existing repository/storage seam — no direct drift calls from UI; autosave must respect `kMaxNoteLength`/tag limits (already enforced by storage).
- Widget tests for: keyboard inset, autosave-on-change (chip + note), failure keeps state, delete confirm unchanged, human-readable titles (Today/Yesterday/absolute).
- Do not touch flow-level semantics — PR #335 (flow model) is in flight elsewhere on `day_sheet.dart`-adjacent code; keep your diff tight to the three defects to minimize merge overlap.

## Gates

```bash
export PATH="/c/src/flutter/bin:$PATH"
flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart
```

## PR

`gh pr create --title "fix(logging): day sheet keyboard inset, autosave with confirmation, human-readable dates (#198)"`, body: `Closes #198 / ## What changed / ## How it satisfies the acceptance criteria / ## Tests / ## Not done`.
