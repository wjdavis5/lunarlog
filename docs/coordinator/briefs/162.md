Worktree C:/git/repos/lunarlog-wt/162-contrast-aa, branch fix/162-contrast-aa, from current main. Issue #162 (read fully via `gh issue view 162`) is the spec: replace colorScheme.outline/tertiary text usages failing WCAG AA contrast. Find every cited site (rg for outline/tertiary in text styles), fix with tokens that pass AA (compute contrast ratios — add a test asserting the token pairs' ratios >= 4.5:1 / 3:1 for large text, following the repo's test style), and keep both themes passing. All copy already localized — no new literals. Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`. PR title: "fix(a11y): WCAG AA contrast for outline/tertiary text usage (#162)".
