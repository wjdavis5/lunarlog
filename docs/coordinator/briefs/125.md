# Task brief — Issue #125: digest cadence and rate limiting for co-guardian alerts

Worktree: `C:/git/repos/lunarlog-wt/125-alert-digest`
Branch: `feat/125-alert-digest` (already created, based on origin/main)
Issue: https://github.com/wjdavis5/lunarlog/issues/125

Read the full issue body (`gh issue view 125`) — it is the spec, including its acceptance-criteria checklist and design constraints. Where this brief and the issue disagree, the issue wins.

## Key starting points (verified to exist on main)

- `supabase/migrations/20260906210000_notification_preferences.sql` — `notification_preferences` table + `push_devices`
- `supabase/migrations/20260906220000_notification_outbox.sql` — content-free outbox, `resolve_deliver_after()` quiet-hours math, `enqueue_caregiver_alerts()` trigger pair (note its writer-exclusion logic — AC3 asks you to verify and pin it with a test)
- `supabase/migrations/20260906230000_reminder_windows_and_cron.sql` — the nightly + 15-min cron wrappers, `sweep_notification_outbox()`, `trigger_push_dispatch()`; the digest sweep should ride this same shape
- `supabase/functions/push-dispatch/` + `supabase/functions/_shared/notification_copy.dart`... (in fact `notification_copy.ts`) — the fixed generic push copy; a digest push must carry the same no-derived-signal text (decide and document whether even a count is allowed — the plan's position: no)
- `lib/domain/notifications/notification_preferences.dart` + `notification_preferences_service.dart` — client preference model to extend with cadence
- Tests to mirror: `supabase/tests/notification_outbox_test.sql`, `missed_entry_scan_test.sql`, `notification_preferences_rls_test.sql`

## Constraints (repo law — read AGENTS.md in the worktree first)

- New migration filenames must sort AFTER `20260907010000_tags_element_length_check.sql` (currently the latest) — use a `20260908…` timestamp. Never edit an applied migration in place; `create or replace` in a new file.
- Migrations are the explicit subject of this issue (allowed). RLS must not be weakened anywhere. The outbox must stay content-free (no column able to hold note/tags/flow/local_date).
- The all-off default stays the default. Cadence is per-guardian, not per-profile.
- Payload rule unchanged: digest pushes carry the same fixed generic strings (`kReminderTitle`/`kReminderBody` equivalents in `_shared/notification_copy.ts` must stay character-identical unless the issue demands otherwise).
- If you touch Drift schema or client models: run `dart run build_runner build --delete-conflicting-outputs` and commit generated code.
- Update AGENTS.md's schema-section line for each new migration (one sentence each, house style).

## Quality gates (must pass before opening the PR)

```bash
export PATH="/c/src/flutter/bin:$PATH"
flutter pub get
flutter analyze                      # zero issues
flutter test                         # all pass
dart run tool/quality_gate.dart      # 90% coverage floor + CRAP gate
# pgTAP needs Docker; if unavailable rely on CI's db-tests job and say so in the PR body
```

## PR

`gh pr create --title "feat(notifications): digest cadence and rate limiting for co-guardian alerts (#125)"` — body template:

```
Closes #125

## What changed
## How it satisfies the acceptance criteria
- [ ] AC1 — <how>   (one line per the issue's checklist)
## Tests
## Not done
```

Squash merge; PR title becomes the commit subject. If you cannot satisfy a criterion, say so under "Not done" rather than silently skipping it.
