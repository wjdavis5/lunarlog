# Task brief — Issue #135: cycle statistics and symptom trends (insights surface)

Worktree: `C:/git/repos/lunarlog-wt/135-insights` — Branch: `feat/135-insights` (created from origin/main)
Issue: https://github.com/wjdavis5/lunarlog/issues/135 — the issue body is the spec. Read it fully first (`gh issue view 135`).

## Coordinator notes (binding)

1. Its "No fertility or ovulation inference, per PRIVACY.md" constraint: the policy reference is stale (#142), but the practical rule STANDS for this issue — descriptive statistics only, no phase inference from symptom timing. Do not reinstate old no-fertility copy anywhere.
2. Sequencing dependencies are satisfied: #132 (cycle history + omit + confidence vocabulary — REUSE `lib/domain/prediction/cycle_history.dart`, do not invent a second confidence vocabulary) and #133 (symptom layer seam `lib/domain/symptoms/symptom_layers.dart` — reuse its most-used ranking) are merged. The #134 taxonomy/intensity work is NOT landed: ship the frequency views on today's boolean tags; leave clear extension seams for intensity later.
3. Charts: prefer direct CustomPaint/widget composition over a charting package; if you add a dependency, justify it in the PR (privacy-review note). Every chart needs a semantic description and a non-visual reading (a11y is in scope).
4. Honest thresholds: minimum-observation gates with loud "not enough yet" states, mirroring `NotEnoughHistory`. Disclaimer on every surface.
5. Pure computation over existing streams; no persisted derived state; no schema/migration changes.

## Starting points

`lib/domain/prediction/cycle_history.dart` (+ its service), `lib/domain/prediction/prediction.dart` (`ActivePrediction` fields), `lib/domain/symptoms/symptom_layers.dart`, `lib/domain/episodes/episodes.dart`, `lib/domain/models/day_entry.dart`, `lib/ui/overview/overview_panel.dart` (entry point), `lib/app.dart` (provider wiring pattern), `lib/observability/route_names.dart` (route constant pattern for the new screen). New pure-Dart stats domain module + a new insights screen; tests mirror under `test/domain/` and `test/ui/`.

## Gates (must pass before the PR)

```bash
export PATH="/c/src/flutter/bin:$PATH"
flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart
```

Plan doc under `docs/plans/` per house convention.

## PR

`gh pr create --title "feat(insights): cycle statistics, symptom patterns, and trends (#135)"`, body template: `Closes #135 / ## What changed / ## How it satisfies the acceptance criteria (checklist) / ## Tests / ## Not done`.
