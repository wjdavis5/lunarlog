Worktree C:/git/repos/lunarlog-wt/249-taxonomy-physical, branch feat/249-taxonomy-physical, from current main. Issue #249 (read fully via `gh issue view 249`) is the spec: the physical-symptom category expansion of the taxonomy (dep #240 TM-2 closed). Codes are permanent; existing codes must not change; new codes stable snake_case; unknown codes never drop (the #162-era vocabulary guard tests plus #237's unrecognised-tags handling on main must keep passing — read both before designing). Coordinate with #131 care modes (mode-keyed category surfacing) and #259 (curation — NOT in scope here beyond leaving clean seams). Client-side taxonomy/domain/day-sheet changes; server impact only if the issue requires (tags remain a string array — the #94 element-length bound (<=64) and 32-count still apply; mirror any new limits in lib/domain/limits.dart).
Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`. Plan doc per house convention.
PR: `gh pr create --title "feat(logging): built-in taxonomy — physical categories (#249)"` — Closes #249 / What changed / ACs / Tests / Not done.
