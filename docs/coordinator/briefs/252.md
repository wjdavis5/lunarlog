Worktree .worktrees/claude-orch/252-taxonomy-events-care, branch claude-orch/252-taxonomy-events-care, from current main. Issue #252 (read fully, `gh issue view 252`) is the spec: taxonomy — events and care categories. Follow the landed #371 (#249) patterns exactly (re-parenting not renaming, option-set-unverified categories ship zero-code behind the caption, import mappings synthetic-tested, clinical terminology rows only when attested). Codes permanent; #251 (feelings/mind/lifestyle, just merged) is a sibling — do not disturb its categories; check current tags.dart first. ARB copy; curation seams (#259, merged) intact.
Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`.
PR: `gh pr create --title "feat(logging): built-in taxonomy — events and care categories (#252)"` — body: Closes #252 / What changed / How it satisfies the acceptance criteria / Tests / Not done.
