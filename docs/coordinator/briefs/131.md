# Task brief — Issue #131: care modes (standard / teen / caregiver / irregular)

Worktree: `C:/git/repos/lunarlog-wt/131-care-modes` — Branch: `feat/131-care-modes` (created from origin/main)
Issue: https://github.com/wjdavis5/lunarlog/issues/131 — the issue body is the spec (R12/U6). Read it fully first (`gh issue view 131`), plus `AGENTS.md`/`CLAUDE.md` in the worktree.

## Settled decisions (do not re-litigate)

1. **Mode is a synced per-profile column**, following the exact precedent of `profiles.birth_year`/`relationship` (migration `20260906160000_profile_subject_metadata.sql`): local Drift schema bump + migration steps (`lib/data/db/tables.dart`, `db.dart` schemaVersion +1, `dart run build_runner build --delete-conflicting-outputs`, commit `db.g.dart`), AND a server migration adding `profiles.mode` (CHECK in `standard|teen|caregiver|irregular`, default `standard`, column grant + `sync_push` key-allowlist/insert/update paths exactly as the precedent did — including the `v_row ? 'mode'` containment-check update pattern from PR #108's review fix). New migration filename must sort after the current tip (check `supabase/migrations/`, now ≥ `20260908110000`).
2. Mode drives vocabulary/defaults/reminder presets **prospectively only**; never permission; never derived from `birthYear`; `teen` is not a reduced app. All per the issue's design constraints — they are hard requirements, tests included.
3. Reminder presets: switching mode requests a replan via the existing coordinator path (`lib/data/notifications/reminder_coordinator.dart`); never rewrite past reminders/entries.

## Starting points

`lib/domain/models/profile.dart` (+ guardians/export surface it feeds), `lib/ui/profiles/profile_dialogs.dart` (create/edit UI), `lib/ui/profiles/profile_detail_screen.dart`, `lib/ui/overview/overview_panel.dart` (status labels / insufficient-history copy), `lib/ui/logging/day_sheet.dart` (category headings), `lib/data/notifications/reminder_coordinator.dart`, `lib/domain/prediction/prediction.dart` (framing; note `irregular` silences the late banner — coordinate with the `LateResolver` from #132 in `lib/ui/overview/late_resolver.dart`). Client sync column path: `lib/data/sync/row_codec.dart` (encode/decode), `lib/data/db/storage.dart` upsert/apply paths.

## Gates (must pass before the PR)

```bash
export PATH="/c/src/flutter/bin:$PATH"
flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart
# pgTAP needs Docker; if unavailable rely on CI db-tests and say so in the PR
```

Update `AGENTS.md` schema line for the new migration; extend pgTAP (`supabase/tests/sync_push_test.sql` pattern from the birth_year coverage) for mode round-trip/reject/no-client-write-bypass. Write a proportional plan doc under `docs/plans/` per house convention.

## PR

`gh pr create --title "feat(profiles): care modes — per-profile vocabulary, defaults, and reminder presets (#131)"`, body template: `Closes #131 / ## What changed / ## How it satisfies the acceptance criteria (checklist) / ## Tests / ## Not done`.
