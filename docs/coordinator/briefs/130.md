# Brief — #130 same-date merge disclosure notice (QUOTA-PAUSED 2026-09-15 11:35Z; resume .worktrees/claude-orch/130-same-date-merge-notice)
Coder ran ~100min — assess partial work, finish.
Pinned architecture: new synced table day_entry_merge_events (profile_id FK cascade, local_date, winning/losing_row_id, field flow|note, losing_value_text <=2000, losing_author_user_id, ULID id, created/updated/deleted_at, server_version). Emission from BOTH resolvers consistently: sync_push same-date branch (current def 20260915200001; carry ALL prior fixes forward when re-emitting; migration sorts after current tip) + LunarLogStorage._resolveSameDateConflicts (rows sync up normally; defaulted jsonb param + key allowlist + v_role_map like p_care_notes). Tags-only and same-id paths emit NOTHING. RLS mirrors day_entries. Dismissal = local per-device store (not tombstone). Retention: piggyback nightly retention job (#263 patterns), suggest 30d, documented. Wire: reconcile_realtime_publication never-publish list, delete_account_data scoping, delete_profile_data (check pattern), export stays client-side (#240 precedent). UI: quiet dismissible notice both guardians see; loser sees recoverable text with restore affordance. Never in push notifications/Sentry. pgTAP + Dart tests per the issue ACs. schemaVersion bump → ALL 6 steps (build_runner, drift_schemas dump, generated_migrations, _kOlderSchemaVersions, ci.yml codegen filename). Gates + PR contract as repo standard; Closes #130.
