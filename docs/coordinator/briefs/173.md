# Task brief — Issue #173: first-party HealthKit/Health Connect platform channel adapter

Worktree: `C:/git/repos/lunarlog-wt/173-health-adapter` — Branch: `feat/173-health-adapter` (created from current origin/main)
Issue: https://github.com/wjdavis5/lunarlog/issues/173 — read it fully (`gh issue view 173`); its decision rationale (option (b), first-party channel, no `health` package) and acceptance checklist are binding.

## Facts to verify first (the issue was written before its deps landed)

- #153 (HS-1 guard), #156 (HS-2 `lib/domain/health/` port start), #166 (HS-3) are merged — locate the actual guard API and any existing `lib/domain/health/`/`lib/data/health/` code on main and BUILD ON IT (do not duplicate or rename what landed). If #156's port shape differs from the issue's sketch, main wins — extend it.
- Check `tool/quality/exclusions.dart` for the precedent entries the issue cites; follow its exact style for the new exclusions (each with a why-comment).

## Scope

1. Platform-neutral `lib/domain/health/` port: one method per data-type concept (menstrual flow, intermenstrual bleeding at minimum; leave clean seams for the later types — cervical mucus, ovulation test, BBT, sexual activity, symptom categories — per the epic's later issues).
2. Dart-side adapters `lib/data/health/ios_health_channel.dart` + `android_health_channel.dart` over one `MethodChannel`, plus the Swift `HKHealthStore` implementation and Kotlin `HealthConnectClient` implementation wired into the existing Runner/MainActivity (study how `lunarlog/privacy` (FLAG_SECURE) and any passkey/health seams landed by #156/#166 are registered — follow that registration pattern exactly).
3. Native side calls the #153 guard before ANY health API touch (do not trust Dart-side gating alone). If the guard currently exists only in Dart, mirror its predicate natively from the same inputs and document the duplication as the safety property the issue demands.
4. No `health` package in pubspec. New native/adapter files → exclusions list with comments. `dart run tool/quality_gate.dart` passes.
5. Dart-testable parts (channel message codec, port logic, guard invocation ordering via a fake MethodChannel) get real tests; pure platform shells stay thin and excluded.
6. Design note recording the (a)-vs-(b) decision (short doc under `docs/` or a thorough header comment — the issue asks for it explicitly).

Platform builds you CANNOT run on this Windows machine (no macOS/Xcode): rely on `flutter analyze` + tests + CI's iOS unsigned build; state any unverifiable wiring under "Not done".

## Gates

```bash
export PATH="/c/src/flutter/bin:$PATH"
flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart
```

Plan doc under `docs/plans/` per house convention.

## PR

`gh pr create --title "feat(health): first-party HealthKit/Health Connect platform channel adapter (#173)"`, body: `Closes #173 / ## What changed / ## How it satisfies the acceptance criteria / ## Tests / ## Not done`.
