# Task brief — Issue #138: a11y — screen-reader and large-text pass (calendar, day sheet, overview)

Worktree: `C:/git/repos/lunarlog-wt/138-a11y-pass` — Branch: `feat/138-a11y-pass` (from current origin/main)
Issue: https://github.com/wjdavis5/lunarlog/issues/138 — read it fully (`gh issue view 138`); it is the spec.

Notes: the surfaces were just reworked (#341 autosave day sheet, #276 forecast calendar with its Semantics seam, #340 l10n with locale-derived labels). Build on those seams; every interactive element reachable and labelled via Semantics/tap targets; text scales (test at 1.5x/2.0x text scale — add widget tests with textScaler); focus order sane; predicted-vs-logged distinction stays semantic (already seeded by #276 — verify and extend to day sheet/overview). Any copy you ADD goes through AppLocalizations (ARB) per #340's pattern — no new hardcoded literals. Do not regress the copy-parity suite.

Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`.
PR: `gh pr create --title "feat(a11y): screen-reader and large-text pass over calendar, day sheet, and overview (#138)"` — body: `Closes #138 / What changed / ACs checklist / Tests / Not done`. Plan doc per house convention.
