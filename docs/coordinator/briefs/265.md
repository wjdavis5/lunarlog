Worktree .worktrees/claude-orch/265-privacyinfo-reconcile, branch claude-orch/265-privacyinfo-reconcile, from current main. Issue #265 (read fully, `gh issue view 265`) is the spec: reconcile PrivacyInfo.xcprivacy required-reason API declarations. Audit the app's actual API usage (UserDefaults via shared_preferences or direct, file timestamps, system boot time, disk space, etc. — the NSPrivacyAccessedAPITypes categories) against the declared reasons; fix declarations to match reality; add a test/CI guard if feasible (a fixture check that the plist parses and every declared reason maps to a real usage). Coordinate with #254's landed health declarations (do not regress them). Gates: standard four. Plan doc per house convention.
PR: `gh pr create --title "fix(ios): reconcile PrivacyInfo.xcprivacy required-reason API declarations (#265)"` — Closes #265 / What changed / ACs / Tests / Not done. Label: owner:claude-orch.
