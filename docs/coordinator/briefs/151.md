Worktree C:/git/repos/lunarlog-wt/151-prediction-connection, branch feat/151-prediction-connection, from current main. Issue #151 (read fully via `gh issue view 151`) is the spec: a prediction-only connection (Clue Connect equivalent) — sharing estimates WITHOUT underlying entry content, distinct from full guardian sharing. Read the existing sharing infrastructure first (lib/data/sharing/, the guardian RPCs in supabase/migrations) and follow its patterns; server changes need a NEW migration (sort after the current tip — check), RLS never weakened, content-free payloads only (an estimate is a number/date, never note/tags/flow detail); pgTAP coverage; client + UI per the issue's ACs. Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`; pgTAP via Docker or CI-noted. Plan doc per house convention. PR title: "feat(sharing): prediction-only connection (#151)", body template Closes #151 / What changed / ACs / Tests / Not done.
