# Brief — #241 ProfileCard + quick switcher (QUOTA-PAUSED 2026-09-15 11:34Z; resume .worktrees/claude-orch/241-profile-card — died ~8min in, little work)
Per gh issue view 241 (deps #182/#126 merged — verify what they shipped): ProfileCard lib/ui/components/profile_card.dart (deterministic hue from profile id, name, one-line status from CyclePredictionService, #126 badges, preserved overflow menu) replacing picker ListTile; app-bar title becomes popup/dropdown switcher with avatars + "Manage profiles…"; month-calendar keeps displayed month across switch when new profile has entries there (pure function for has-entries-in-month; today-reset when not). Status copy via l10n arb — regenerate AND commit (PR #714 failed CI on exactly this). Theme tokens only (architecture guard). Tests: card renders per prediction state, switcher switches, month-preservation, menu preserved. Gates: analyze, full test, quality_gate. PR: owner:claude-orch, Closes #241.
