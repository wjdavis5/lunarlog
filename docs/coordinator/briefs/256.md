Worktree C:/git/repos/lunarlog-wt/256-intensity-model, branch feat/256-intensity-model, from current main. Issue #256 (read fully, `gh issue view 256`) is the spec: intensity/severity model for tracked observations. Dep #240 closed; the observations table (#240) and the #371 taxonomy are landed. Storage per the issue's direction (observation-scoped intensity on the observations table — follow the landed shape; if the issue allows alternatives document the choice). The severity consumer (caregiver-alert trigger's `flow='heavy'` proxy replacement) may be part of this issue or a follow-up — follow the issue text; if in scope, `create or replace` in a NEW migration rebased on the current trigger definition, with pgTAP. Schema bump => FULL procedure (build_runner regen + drift_schema_v<N+1>.json + ci.yml dump-filename bump — missed twice repo-wide). Sync containment guards per house pattern; pgTAP round-trip; unknown-code/legacy-entry semantics unambiguous per the issue. Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`; pgTAP via Docker or CI-noted. Plan doc per house convention. PR: `gh pr create --title "feat(logging): intensity/severity model for tracked observations (#256)"` — Closes #256 / What changed / ACs / Tests / Not done.
