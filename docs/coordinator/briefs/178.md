Worktree C:/git/repos/lunarlog-wt/178-reminder-kinds, branch feat/178-reminder-kinds, from current main. Issue #178 (read fully via `gh issue view 178`) is the spec: period-starting-soon, fertile-window-soon, and cycle-statistic-change reminders. Deps #143/#213 landed. Build on the JUST-LANDED #136 per-type configurable reminder architecture (ReminderKind/config store/reminder_coordinator/action executor — extend, never fork); new kinds are separately configurable, fertile-window-soon consumes #143's estimation, statistic-change fires on meaningful changes to the #132/#220 statistics. Copy through ARB; generic notification copy only (no health detail on lock screens). No schema/migration changes expected (device-local configs) — if the issue demands otherwise, house rules. Reminder wiring NOT duplicating #125's caregiver path.
Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`. Plan doc per house convention.
PR: `gh pr create --title "feat(notifications): period-starting-soon, fertile-window-soon, and statistic-change reminders (#178)"` — Closes #178 / What changed / ACs / Tests / Not done.
