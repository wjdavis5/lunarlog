# Task brief — Issue #122: reposition product framing around sync and family collaboration (+ closes #150)

Worktree: `C:/git/repos/lunarlog-wt/122-reposition` — Branch: `feat/122-reposition` (created from origin/main)
Issues: https://github.com/wjdavis5/lunarlog/issues/122 (spec) and https://github.com/wjdavis5/lunarlog/issues/150 (folded in — same stale SQLCipher wording in the hosted policy).

Read both issue bodies first (`gh issue view 122`, `gh issue view 150`). #122 lists exact files and line ranges — verify each against current main (the repo has moved since the issue was written; find the equivalent passages).

## Coordinator notes (binding)

1. Positioning correction, not feature removal: offline/encryption/gate/no-tracking all stay as properties; the pitch leads with sync + family collaboration. Follow the issue's per-file scope list (README, PRIVACY.md, AGENTS.md, first-run copy, roadmap authority note). Where line numbers have drifted, fix the equivalent passage.
2. **Fold #150 in**: `web/privacy.html` (the hosted policy) gets the same positioning + encryption-wording corrections you make to `PRIVACY.md` — its "Encrypted at Rest ... SQLCipher (AES-256)" bullet is factually wrong since #118 and must match PRIVACY.md's OS-at-rest wording. The PR body carries `Closes #122` AND `Closes #150`.
3. PRIVACY.md internal contradictions the issue calls out (L31 vs L15) get reconciled; bump `Last Updated` and add a Change History line per the policy's own convention (see the September 7 entry for format). Keep the fertility-amendment text (#142) intact — do not regress it.
4. Truthful present-tense: sync exists, sharing exists, alerts exist — these can be pitched as shipped. Nothing unreleased gets claimed.
5. `docs/plans/` references to "local-first" authority: update the roadmap's authority note only where it misstates the product's identity; historical plan docs stay as history (do not rewrite old plans beyond the roadmap authority note the issue names).

## Gates (must pass before the PR)

```bash
export PATH="/c/src/flutter/bin:$PATH"
flutter pub get && flutter analyze && flutter test   # copy edits must not break tests (first-run copy is widget-tested)
```

Small plan/notes doc optional for a docs-only change; skip if the PR body suffices.

## PR

`gh pr create --title "docs: reposition product framing around sync and family collaboration (#122, #150)"`, body template: `Closes #122` + `Closes #150` / `## What changed / ## How it satisfies the acceptance criteria / ## Tests / ## Not done`.
