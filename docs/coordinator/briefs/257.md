Worktree .worktrees/claude-orch/257-custom-tags, branch claude-orch/257-custom-tags, from current main. Issue #257 (read fully, `gh issue view 257`) is the spec: per-profile custom-tag registry. Deps landed (#240 observations, #371/#381 taxonomy, #224 tombstone semantics). Registry synced opaquely per the issue (follow its storage direction — coordinate with #259's landed profiles.tracking_preferences jsonb if the issue allows that shape, else its own keyed structure with containment-guarded sync path); validation accepts registry members; unknown synced codes render as raw text (the #237 unrecognised-tags machinery on main is your integration point — read it first); case-insensitive dedupe with per-profile cap; the #94 element-length bound (<=64) applies. Schema bump => FULL procedure (regen + dump + ci.yml filename bump; use a timestamp sorting after everything on main — the tip moves hourly). Gates: standard four (analyze/test/quality_gate + pgTAP via Docker or CI-noted). Plan doc per house convention.
PR: `gh pr create --title "feat(logging): per-profile custom-tag registry (#257)"` — Closes #257 / What changed / ACs / Tests / Not done. Label: owner:claude-orch.
