# Brief — #42 perf(sync): batch storage operations (QUOTA-PAUSED 2026-09-15 11:40Z; resume partial work in .worktrees/claude-orch/42-sync-batching)
Coder ran ~2h before the limit kill — assess existing partial work first, finish rather than restart.
1. Batched markPushed (single Drift transaction) preserving the local_rev guard exactly (lib/data/sync/supabase_sync_engine.dart).
2. Replace per-row N+1 SELECTs in remote apply with per-page chunked IN(...) lookups (SQLite var cap; lib/data/db/storage.dart).
3. max(server_version) probe before daily full reconcile re-pull; on probe error fall back to full re-pull (never silently skip).
Tests: batched markPushed respects local_rev (concurrent change not marked pushed); apply resolves 500-row pages incl. new/updated/deleted; probe skips unchanged + falls back on error. Gates: analyze, full flutter test, quality_gate (lib/ coverage floor + CRAP). PR: owner:claude-orch label, Closes #42, head claude-orch/42-sync-batching.
