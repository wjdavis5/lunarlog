Worktree C:/git/repos/lunarlog-wt/136-reminder-actions, branch feat/136-reminder-actions, from current main. Issue #136 (read fully via `gh issue view 136`) is the spec: per-type configurable reminders with log-from-notification actions. Build on the existing notification stack (lib/data/notifications/, reminder_coordinator, notification preferences screen, the #125 cadence work, FCM/local-notification paths — check what landed and reuse; do not fork a second preferences model). Log-from-notification actions are native-side (notification action buttons → deep link/payload the gate already handles: pendingLaunchProfileId); keep actions working within the existing privacy posture (generic copy only). No schema/migration changes expected — if needed, house rules + sort after tip. Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`. Plan doc per house convention. PR title: "feat(notifications): per-type configurable reminders with log-from-notification actions (#136)".
