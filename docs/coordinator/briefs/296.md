Worktree C:/git/repos/lunarlog-wt/296-health-binding-prereqs, branch fix/296-health-binding-prereqs, from current main. Issue #296 (read fully, `gh issue view 296`) is the spec: the prerequisites before healthSyncMinorBindingAllowed can be flipped (transfer target, birth-year boundary, pinned default). This is SAFETY-CRITICAL gate logic (the #153 guard this whole epic relies on) — implement exactly the issue's three prerequisites with tests pinning each; no behavior flip in this PR beyond what the issue authorizes; be conservative: if the issue and the current code disagree about the flag's current default, the SAFER default wins and the discrepancy goes in the PR body. #193's health-write work just landed on main — build on the current lib/domain/health/ shape. No schema changes expected. Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`. PR: `gh pr create --title "fix(health): prerequisites before healthSyncMinorBindingAllowed can be flipped (#296)"` — Closes #296 / What changed / ACs / Tests / Not done.
