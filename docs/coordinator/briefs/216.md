# Task brief — Issue #216: onboarding — identity/value cards and cycle questions in first run

Worktree: `C:/git/repos/lunarlog-wt/216-onboarding` — Branch: `feat/216-onboarding` (from current origin/main)
Issue: https://github.com/wjdavis5/lunarlog/issues/216 — read it fully (`gh issue view 216`); its card list, cycle-questions table, and skippability requirements are the spec.

Notes: first_run was just repositioned copy-wise (#334) and care modes (#131) + modes storage (#188) landed — the minor-checkbox explanation card can state what it ACTUALLY does now (care-mode vocabulary + life-stage mode are separate; keep the explanation truthful and brief). All new copy through AppLocalizations/ARB (#340 pattern) — no hardcoded literals; the copy-parity suite must stay green. Cycle-question answers seed per the issue (last period date, typical lengths) — check whether the issue directs storage into reminders/prediction seeding and follow it exactly; where it defers, leave clean seams and note it. Illustrations from #164 do NOT exist yet — do not block on them (text-only cards fine, note under Not done).

Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`.
PR: `gh pr create --title "feat(onboarding): identity/value cards and cycle questions in first run (#216)"` — body: `Closes #216 / What changed / ACs checklist / Tests / Not done`. Plan doc per house convention.
