# Task brief — Issue #242: ownership_transfers.token_hash is table-readable and redeemable

Worktree: `C:/git/repos/lunarlog-wt/242-ownership-token-hash-unreadable`
Branch: `fix/242-ownership-token-hash-unreadable` (already created, based on origin/main)
Issue: https://github.com/wjdavis5/lunarlog/issues/242

## Task

Mirror the fix that just merged as PR #219 for issue #114 (`supabase/migrations/20260908000000_guardian_invitations_token_hash_unreadable.sql` — read it first; it is the template) onto `ownership_transfers`:

1. New migration `supabase/migrations/20260908100000_ownership_transfers_token_hash_unreadable.sql` (timestamp must sort after every existing migration — verify the current tip first, which includes 20260908000000): revoke the table-wide `SELECT` on `public.ownership_transfers` from `authenticated`; re-grant `SELECT` on every column EXCEPT `token_hash` (enumerate from the actual schema in `20260906170000_ownership_transfers.sql`; verify no later migration added columns). Header comment documents the attack (any accepted primary_guardian can read a live transfer's hash and redeem it via `accept_ownership_transfer`, moving profile ownership), cites #242, and follows the house migration style.
2. Verify no client path selects `token_hash` or `*` from `ownership_transfers` (`rg -n "ownership_transfers" lib/ supabase/functions/`); state the result in the PR body. The transfer flows run through RPCs (`lib/data/sharing/` or `lib/domain/` — find them); if any reader exists that would break, STOP and report under "Not done".
3. pgTAP coverage mirroring PR #219's additions: an authenticated primary_guardian (non-initiator) CANNOT read / filter on / star-expand `token_hash` (42501), CAN still select the granted columns of the transfer row; the arming parent's legitimate reads and the full create → accept / create → cancel RPC lifecycle still pass. Extend the existing ownership-transfer test file (`supabase/tests/ownership_transfer_test.sql`) rather than adding a new one if it fits, matching its fixtures. If any existing tests select `token_hash` from an authenticated context (the way #219 had to fix `where token_hash = …` sub-selects with an owner-context helper), replace them with owner-context helpers as PR #219 did in `000-setup.sql`.
4. Update AGENTS.md's schema-section line for this migration (one sentence, house style).

## Commands (Windows Git Bash)

```bash
export PATH="/c/src/flutter/bin:$PATH"
flutter pub get && flutter analyze && flutter test    # no Dart changes expected; must stay green
# pgTAP needs Docker; if unavailable rely on CI's db-tests job and say so in the PR body
```

## PR

`gh pr create --title "fix(security): make ownership_transfers.token_hash unreadable to clients (#242)"` — body template:

```
Closes #242

## What changed
## How it satisfies the acceptance criteria
- [ ] AC1 — <how>
## Tests
## Not done
```
