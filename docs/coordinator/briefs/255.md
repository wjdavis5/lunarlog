Worktree C:/git/repos/lunarlog-wt/255-numeric-measurements, branch feat/255-numeric-measurements, from current main. Issue #255 (read fully, `gh issue view 255`) is the spec: BBT and weight as numeric measurements. Dep #240 closed. Schema bump likely (new measurement storage) — FULL procedure: build_runner regen, drift_schema_v<N+1>.json dump, AND the ci.yml codegen-freshness filename bump (missed twice repo-wide; do not repeat). Server side follows the observations/pms precedents (containment-guarded sync_push, CHECKs). Sync round-trip + import/export coverage required. Units: per-profile display preference, independent of stored unit (issue AC). Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`; pgTAP via Docker or CI-noted. Plan doc per house convention. PR: `gh pr create --title "feat(logging): numeric measurements — BBT and weight (#255)"` — Closes #255 / What changed / ACs / Tests / Not done.
