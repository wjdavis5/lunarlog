# Task brief — Issue #188: profile_modes and cycle_overrides storage, RLS, and sync

Worktree: `C:/git/repos/lunarlog-wt/188-modes-storage` — Branch: `feat/188-modes-storage` (created from current origin/main)
Issue: https://github.com/wjdavis5/lunarlog/issues/188 — read it fully (`gh issue view 188`), including its exact DDL sketches and the orthogonality note vs #131 (`profiles.mode` care modes already exist — do NOT touch or collapse them).

## Scope

Two new synced tables following the existing `profiles`/`day_entries` pattern end to end:

1. **Server** — new migration (timestamp must sort after the current tip — check `supabase/migrations/`, several parallel PRs are adding migrations; pick a clearly-later `2026 09 09…` timestamp):
   - `public.profile_modes` (DDL in the issue; note the 5-value mode enum incl. `conceive`, and `health_sync_consent`), `public.cycle_overrides` (DDL continues in the issue body — read the rest).
   - RLS enabled+forced; policies: read = the profile's accepted guardians; write = per the issue's authority model (primary/co-parent edit; follow the issue text). Column grants minimal; `server_version` trigger from `sync_version_seq`; indexes on the pull path (`(user_id…, server_version)` — follow the existing pattern; note these tables key on `profile_id`, so scope policies via `is_profile_guardian`/role helpers like `profile_guardians` does).
   - `sync_push` extension: new optional params are a breaking RPC change — instead follow how `profile_guardians` is handled (server-authoritative, NOT pushed via sync_push) UNLESS the issue's pattern section says otherwise. **Read the issue's sync section carefully and follow it.** If it demands sync_push params, extend with defaulted params (`p_profile_modes jsonb default '[]'`, etc.) so old callers still work, mirroring the key-allowlist/validation style of the current body (rebase on the CURRENT definition — check every migration that last defined sync_push).
   - Pull surface: the client pulls via PostgREST selects like the other tables.
2. **Client** — Drift schema v-next (tables + `sync_state` cursor columns for the two new tables), storage upsert/apply paths with LWW + tombstones per house rules (or the row-replace semantics the issue specifies for one-row-per-profile tables), row codec encode/decode, `SyncTable` extension + engine pull/push wiring (per-table cursors, checkpointing), and a minimal domain model. UI is NOT in scope beyond what the issue requires (no mode-picker screens — storage/sync only).
3. pgTAP: RLS isolation (guardian vs stranger vs anon), write authority, sync path round-trip + old-client omission safety (containment-guard pattern where rows are pushed), CHECK enforcement. Mirror `profile_guardians_rls_test.sql`/`sync_push_test.sql` fixture style.
4. `AGENTS.md` schema lines + test counts. `dart run build_runner build --delete-conflicting-outputs`, commit `db.g.dart`.

## Constraints

- Migrations additive only; RLS never weakened; content-free logs; `search_path = ''` on functions; SECURITY DEFINER only where the issue's authority model requires it (and revoke execute from anon).
- If any part of the issue's DDL conflicts with current main reality (e.g., a parallel PR already added one of these tables or columns), STOP that part, note it under "Not done", and land the rest.

## Gates

```bash
export PATH="/c/src/flutter/bin:$PATH"
flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart
# pgTAP needs Docker; if unavailable rely on CI db-tests and note it in the PR
```

Plan doc under `docs/plans/`.

## PR

`gh pr create --title "feat(modes): profile_modes and cycle_overrides storage with RLS and sync (#188)"`, body: `Closes #188 / ## What changed / ## How it satisfies the acceptance criteria / ## Tests / ## Not done`.
