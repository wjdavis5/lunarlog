# Task brief — Issue #197: perf — month-range day-entry queries and missing indexes

Worktree: `C:/git/repos/lunarlog-wt/197-calendar-perf` — Branch: `feat/197-calendar-perf` (from current origin/main)
Issue: https://github.com/wjdavis5/lunarlog/issues/197 — read it fully (`gh issue view 197`); it is the spec (calendar now navigates 12 months forward via #276, so per-month query costs compound).

Notes: this is Drift/query + index work in `lib/data/db/` (and any watch-query shapes in `lib/ui/logging/month_calendar.dart` / repositories). Measure first (write a benchmark-ish test demonstrating the N-month cost before/after if feasible); add the missing indexes via the schema (schemaVersion bump per house procedure — codegen freshness: `dart run build_runner build --delete-conflicting-outputs`, dump `drift_schema_v<new>.json`, AND bump the filename in `.github/workflows/ci.yml`'s codegen-freshness step — the step's own comment explains; this exact miss failed a PR today). No server/migration changes expected (local indexes only) — if the issue demands server indexes, follow the migration house rules and rebase on the current tip.

Gates: `export PATH="/c/src/flutter/bin:$PATH"; flutter pub get && flutter analyze && flutter test && dart run tool/quality_gate.dart`.
PR: `gh pr create --title "perf(calendar): month-range day-entry queries and missing indexes (#197)"` — body: `Closes #197 / What changed / ACs checklist / Tests / Not done`. Plan doc per house convention.
