# Shell scripts under .github/scripts/ are executed by GitHub Actions
# Linux/macOS runners and must never pick up CRLF line endings from a
# Windows-authored commit (see issue #41, U5).
*.sh text eol=lf
